#include "LedSnakeMatrix.h"

/*std::shared_ptr<LedPixel> LedSnakeMatrix::getPixel(uint16_t x, uint16_t y) const
{
    uint16_t index = x * _height + (x % 2 == 0 ? y : _height - y - 1);
    //return LedStrip::getPixel(index);
    return nullptr;
}*/